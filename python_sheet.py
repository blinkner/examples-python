import numpy
import math
import random

random.random() # Float aleatório entre 0 e 1.
random.uniform(inicio, fim) # Float aleatório entre início e fim.
random.randint(inicio, fim) # Inteiro aleatório entre início e fim.
random.randrange(inicio, fim, passo) # Inteiro aleatório entre início e fim com passo.
range(inicio, fim, passo)
list()
input("Entre com um valor: ") # Entrada de dados



def funcao(): # Função
    return 0

