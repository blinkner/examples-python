import numpy
import math
import random

random.random() # Inteiro aleatório entre 0 e 1.
random.uniform(inicio, fim) # Inteiro aleatório entre início e fim.
random.randint(inicio, fim)
random.randrange(inicio, fim, passo)

